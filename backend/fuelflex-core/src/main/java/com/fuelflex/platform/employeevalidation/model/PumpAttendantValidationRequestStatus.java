package com.fuelflex.platform.employeevalidation.model;

public enum PumpAttendantValidationRequestStatus {
    DRAFT,
    PENDING_SUPERVISOR_APPROVAL,
    RETURNED_FOR_CORRECTION,
    VALIDATED,
    REJECTED,
    CANCELLED
}
