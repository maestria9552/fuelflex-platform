package com.fuelflex.platform.user.model;

public enum PumpAttendantValidationStatus {
    PREPARATION,
    PENDING_SUPERVISOR_APPROVAL,
    RETURNED_FOR_CORRECTION,
    VALIDATED,
    REJECTED,
    CANCELLED
}
